<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
        <!-- Kiosk-style always-on display: dark window background so any
             inset/system-bar area never flashes the AppCompat default white. -->
        <item name="android:windowBackground">@color/deskAgentBg</item>
        <item name="android:statusBarColor">@color/deskAgentBg</item>
        <item name="android:navigationBarColor">@color/deskAgentBg</item>
    </style>

</resources>
